import { styles } from "~/app/styles";

export default function ContributePage() {
  return (
    <div className={`${styles.paddingX} ${styles.flexCenter}`}>
      <div className={`${styles.boxWidth}`}>Contribute</div>
    </div>
  );
}
