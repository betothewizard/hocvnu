import { styles } from "~/styles";

export default function Quizzes() {
  return (
    <div className={`${styles.paddingX} ${styles.flexCenter}`}>
      <div className={`${styles.boxWidth}`}>Gi</div>
    </div>
  );
}
